This project is based on two container images which are (a)mysql:5.7 and (b)postfixadmin:latest.
For the above image you could download from https://hub.docker.com/ and search for the respectives image.
If this issue arises "Warning: Depends on: IMAP functions - NOT FOUND"

If the project is successfully run please click on this link:
http://localhost:8080/setup.php
setup password is topsecret99

Required os:-
RehHat8,docker ce and ,docker-compose.

command "docker-compose up -d"
